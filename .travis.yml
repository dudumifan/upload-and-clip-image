language: node_js
node_js:
  - '10'

# Travis-CI Caching
cache:
  directories:
    - node_modules

# S:Build Lifecycle
install:
  - npm install

script:
  - npm run test:e2e

after_success:
  - npm run coverage